not computable: no linear-interpolation reconstruction is present in the read-only artifacts. agent_a/reconstruction_series.parquet stores only truth, reconstruction (XGBoost) and climatology series (columns: network_id, station_id, gap_length, placement, date, truth, reconstruction, climatology); agent_a/placement_thermal_metrics.csv stores errors for reconstruction and climatology only; agent_b/placement_metrics.csv stores truth/recon/missing/clim metrics but no interp fill. Computing err_interp would require re-running the downstream metric pipeline (window definition, phase smoothing, trend OLS) on a newly created linear-interpolated fill, which is outside this read-only artifact analysis.
